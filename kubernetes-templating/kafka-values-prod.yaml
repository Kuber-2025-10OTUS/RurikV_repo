# Kafka values for PROD environment
# - 5 brokers, 3 controllers (scaled down for kind demo)
# - SASL_PLAINTEXT protocol
# - Latest Kafka version (tag not specified = latest)

image:
  registry: docker.io
  repository: bitnamilegacy/kafka
  # tag not specified to use the latest available version

kraft:
  enabled: true
  clusterId: "kafka-prod-cluster-001"

# 3 controllers for quorum, 5 brokers for production
# Note: For local kind cluster testing, you may want to scale down
controller:
  replicaCount: 1

broker:
  replicaCount: 1

listeners:
  client:
    protocol: SASL_PLAINTEXT
  controller:
    protocol: SASL_PLAINTEXT
  interbroker:
    protocol: SASL_PLAINTEXT

sasl:
  enabled: true
  client:
    users:
      - app_user
    passwords:
      - app_password
  interbroker:
    password: interbroker_secret_pass
  controller:
    password: controller_secret_pass

zookeeper:
  enabled: false

persistence:
  size: 8Gi
